spring:
  application:
    name: auth-service
  config:
    import: configserver:https://user:password@icu-config.herokuapp.com

jasypt:
  encryptor:
    password: jazaradebuggers

swagger:
  config:
    refreshrate: 5000