spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: https://icu-config.herokuapp.com
      username: user
      password: password

jasypt:
  encryptor:
    password: jazaradebuggers